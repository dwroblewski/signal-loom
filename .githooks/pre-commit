#!/usr/bin/env bash
# signal-loom pre-commit: block secrets / personal data before they enter history.
# Enabled via: git config core.hooksPath .githooks  (see scripts/setup-hooks.sh)
exec "$(git rev-parse --show-toplevel)/scripts/check-leaks.sh" staged
